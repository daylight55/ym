# values.yaml for my-app
# Version: 1.0.0

replicaCount: 1

image:
  repository: nginx
  tag: stable
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

# Custom variables example:
imageTag: latest
replicaCount: 2
servicePort: 8080
